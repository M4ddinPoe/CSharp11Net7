// See https://aka.ms/new-console-template for more information

IntPtr intPtr = IntPtr.Zero;
// same as
nint intPtr2 = IntPtr.Zero;

UIntPtr uIntPtr = UIntPtr.Zero;
// same as
nuint uIntPtr2 = UIntPtr.Zero;